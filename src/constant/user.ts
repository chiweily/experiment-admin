// 用户添加/编辑页面用户信息数据

export enum USER_SEX {
    MALE = "male",
    FEMALE = "female"
}

export enum USER_ROLE {
    USER = "user",
    ADMIN = "admin"
}

export enum USER_STATUS {
    ON = "on",
    OFF = "off"
}